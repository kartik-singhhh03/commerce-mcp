/**
 * Inventory domain service.
 *
 * Responsibility:
 * - Resolve reservation / stock status for an order's SKUs
 * - Surface pick_blocked, out_of_stock, etc. as data — never explain "why"
 */

// TODO: Export getInventoryStatus(orderId: string): InventoryStatus

export {};
