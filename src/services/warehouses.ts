/**
 * Warehouse domain service.
 *
 * Responsibility:
 * - Resolve warehouse health / events for an order's assigned facility
 */

// TODO: Export getWarehouseStatus(orderId: string | warehouseId: string): WarehouseStatus

export {};
