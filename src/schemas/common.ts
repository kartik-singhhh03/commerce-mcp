/**
 * Shared Zod primitives and ID normalizers.
 *
 * Responsibility:
 * - orderId / sku / warehouseId / caseId schemas
 * - Preprocess helpers (strip "#", trim whitespace)
 */

// TODO: Export orderIdSchema, skuSchema, warehouseIdSchema, caseIdSchema
// TODO: Use zod/v4 (Standard Schema compatible with MCP SDK v2)

export {};
