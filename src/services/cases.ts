/**
 * Operations case domain service.
 *
 * Responsibility:
 * - Create and retrieve ops cases linked to orders
 * - Enforce case field requirements and open-case uniqueness (when implemented)
 *
 * Does NOT:
 * - Decide root cause (AI supplies summary/rootCause via tool input)
 */

// TODO: Export createOperationsCase(input): OperationsCase
// TODO: Export getOperationsCase(caseId | orderId): OperationsCase

export {};
