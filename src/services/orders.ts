/**
 * Order domain service.
 *
 * Responsibility:
 * - Look up orders by normalized order number
 * - Return deterministic order facts only (no root-cause narrative)
 */

// TODO: Export getOrder(orderId: string): Order
// TODO: Throw NotFoundError when missing

export {};
