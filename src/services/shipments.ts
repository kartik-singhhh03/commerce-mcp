/**
 * Shipment domain service.
 *
 * Responsibility:
 * - Resolve shipment / label / carrier state for an order
 * - Represent "not created" as an explicit deterministic result when no row exists
 */

// TODO: Export getShipmentStatus(orderId: string): ShipmentStatus

export {};
