/**
 * Optional smoke client against a running MCP endpoint (local or Railway).
 *
 * Responsibility:
 * - Connect via Streamable HTTP client transport
 * - List tools and call get_order for the demo order
 *
 * Official SDK (v2):
 *   import { Client } from '@modelcontextprotocol/client';
 */

// TODO: Read MCP_URL from env (default http://127.0.0.1:3000/mcp)
// TODO: List tools, call get_order({ orderId: '1234' }), print JSON

export {};
