/**
 * MCP tool: get_order
 *
 * Thin adapter: Zod input → orders service → structured tool result.
 * No investigation logic.
 */

// TODO: Export registerGetOrder(server: McpServer): void
// TODO: server.registerTool('get_order', { description, inputSchema }, handler)

export {};
